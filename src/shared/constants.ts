/** App-wide constants. */
export const CONSTANTS = {
  /** localStorage key for backend base URL. */
  BACKEND_URL_STORAGE_KEY: 'pokepvp-backend-base-url',
  /** localStorage key for music muted preference. */
  MUSIC_MUTED_STORAGE_KEY: 'pokepvp-music-muted',
} as const
