# Business Rules — PokePVP (stub)

**Canonical documentation lives in the Obsidian vault** (edit there, not a full duplicate in Git):

`/home/victorr/Documents/dev/dev-notes/Projects/PokePVP Front/01 - Business rules.md`

**Summary:** Implementation-agnostic rules for catalog, team selection (nickname-only entry, 3 random Pokémon per player, no duplicates across players), lobby states (`waiting` → `ready` → `battling` → `finished`), turn-based battle flow, damage formula, and the client–server Socket.IO contract.

For the project index in the vault, see `00 - PokePVP Front project.md` in the same folder.
